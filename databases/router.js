'use strict';
const express = require('express');
const dbModels = require('./models');
const config = require('../config');
const router = express.Router();


//router.route('/:username', (req, res) => {
  //
router.route('/:username')
  .get((req,res)=>{

  });
  


module.exports = {router};